# 業務オーケストレーション方針（品質優先 → スピード）

このプロジェクトの主業務はソフトウェア開発ではなく、次の3つの知的生産タスク:

1. **補助金で採択される事業計画書の策定** → `/business-plan` スキルに従う
   （主対象: 中小企業新事業進出補助金・ものづくり補助金・中小企業成長加速化補助金）
2. **人材開発支援助成金に対応した生成AI研修カリキュラム作成とLMS化** → `/training-curriculum` スキルに従う
3. **プロ品質のLP・HP制作** → `/landing-page` スキルに従う

基本思想: **高品質は「一発で良い成果物を書く」ことではなく、
「速く下書きし、"落とす側の視点" で厳しくレビューし、磨き直す」ループで作る。**
どんなに急いでいても、Opus レビューを通さない納品は禁止。

## モデル・ルーティング表

| 工程 | モデル | 理由 |
|------|--------|------|
| 資料収集・下調べ・既存資産の棚卸し | Haiku（`scout`・並列起動） | 読むだけの作業は最速・最安で十分 |
| 執筆・教材作成・コーディング・修正 | Sonnet 5（メインループ） | 生産速度と品質のバランスが最良 |
| 構成設計・戦略立案・アプローチの裁定 | Opus（`architect`） | 構成ミスは全文書き直しになるため品質優先 |
| 補助金の審査員視点レビュー | Opus（`grant-examiner`） | 採択可否を分ける最重要工程 |
| 助成金の要件適合チェック | Opus（`compliance-checker`） | 不支給リスクの最終防衛線 |
| LP/HP のクリエイティブレビュー | Opus（`lp-director`） | CVRを決める判断はプロの目で |
| コードの最終レビュー | Opus（`deep-reviewer`） | 出荷前の最後の砦 |
| 制度・数値のファクトチェック | Sonnet（`fact-checker`・Web検索可） | 一次ソース照合は網羅性が命 |

**原則: Opus は「審査・診断・判断」に使い、手を動かすのは Sonnet 5。**
Opus には評価結果と改善指示を返させ、書き直しはメインループが行う。

## 共通品質パイプライン（全ドメイン共通）

1. **Research**: 一次ソース収集と既存資産の棚卸し。観点が複数あれば `scout` と
   `fact-checker` を**1メッセージで並列起動**する。
2. **Plan（Opus `architect`）**: 書き始める前に「評価基準・構成・戦略」を確定させる。
   評価基準（審査項目/支給要件/CV目標）が不明なまま Draft に進むことを禁止する。
3. **Draft（Sonnet 5・メインループ）**: 計画に沿って一気に作る。逐一確認で止まらない。
4. **Adversarial Review（Opus）**: ドメイン対応のレビュアーに「落とす側」として
   評価させる（審査員 / 労働局 / 離脱するユーザー）。
5. **Revise → 再Review**: 重大指摘がゼロになるまでループ。**最大3周**。
   3周で収束しない場合は Draft の修正ではなく Plan（構成）に戻る。
6. **Final Check**: スキル記載のチェックリスト全項目 + 出典確認を通してから納品。

## ファクト規律（品質の生命線・全成果物に適用）

- 補助金・助成金の要件、金額、補助率、締切は**必ず当年度の公募要領・支給要領
  （一次ソース）を `fact-checker` で確認**し、出典URLと確認日を成果物に記録する。
- 自分の記憶にある制度知識は「古い可能性がある仮説」として扱う。制度は毎年変わる。
- 市場データ・統計は出典を明記する。出典が確認できない数値は成果物に載せない。
- **捏造の絶対禁止**: 存在しない統計、架空の顧客事例、根拠のない実績は書かない。
  クライアントの実情（売上・体制・実績）が不足しているときは、それらしく埋めずに
  ユーザーに質問する。**ここだけは質問がスピードより優先される。**

## 自律行動の規範

- 成果物は必ずスキル記載の検証（チェックリスト / 実ブラウザ確認 / 要件照合）を
  **自分で**実行してから完了とする。「たぶん大丈夫」で終えない。
- レビュー指摘は質問せずに修正 → 再レビュー。ただし3周ルール（上記）を守る。
- 表現・構成など妥当性が同程度の選択肢は片方を選んで進め、理由を最後に一言報告する。
- ユーザーに質問してよいのは: クライアントの実情データが必要なとき、要件そのものが
  曖昧なとき、破壊的操作の前、スコープが明らかに変わるときのみ。
- 完了の条件: 作成済み・ファクト確認済み・Opusレビュー指摘対応済み・チェックリスト通過。

## スピードのための規則

- **並列化を常に検討**: 独立した調査・章ごとのファクトチェック・複数観点のレビューは
  同一メッセージで複数エージェントを起動する。
- **サブエージェントに生データを持ち帰らせない**: 結論と根拠の要約だけ返させ、
  メインのコンテキストを汚さない（コンテキスト肥大は品質と速度の両方を落とす）。
- **Opus を待ち時間にしない**: architect が構成を練っている間に、確定している
  準備作業（資料整理、テンプレート準備、開発環境セットアップ）を先に進める。
- Opus 呼び出しは1成果物あたり目安 3〜4回（Plan 1回 + Review 2〜3周）。
  それ以上必要になるのは、Plan の粒度が粗いサイン。

## サブエージェント定義（`.claude/agents/`）

| エージェント | モデル | 役割 |
|--------------|--------|------|
| `scout` | haiku | 読み取り専用の資料・コード・既存資産の調査 |
| `architect` | opus | 成果物の構成設計・戦略立案・アプローチの裁定 |
| `grant-examiner` | opus | 補助金審査員視点の敵対的レビュー（採点つき） |
| `compliance-checker` | opus | 助成金支給要件との適合性チェック |
| `lp-director` | opus | LP/HP のクリエイティブディレクターレビュー |
| `fact-checker` | sonnet | 制度・統計・数値の一次ソース照合（Web検索可） |
| `deep-reviewer` | opus | コード差分の敵対的レビュー（LP/LMS実装用） |
| `debugger` | opus | 2回修正に失敗したバグの根本原因分析 |

## スキル定義（`.claude/skills/`）

| スキル | 用途 |
|--------|------|
| `/business-plan` | 補助金事業計画書の策定パイプライン |
| `/training-curriculum` | 助成金対応の生成AIカリキュラム作成 + LMS化 |
| `/landing-page` | プロ品質 LP/HP の制作パイプライン |

該当業務を依頼されたら、**最初に対応するスキルを起動**し、その手順に従うこと。

## リポジトリ内のツール検証（scan-secrets.sh を触るときのみ）

- 構文チェック: `bash -n scan-secrets.sh` / Lint: `shellcheck scan-secrets.sh`
- 動作確認: `./scan-secrets.sh .` を実行し exit code と出力を確認（ripgrep `rg` が必要）
