# CLAUDE.md

このリポジトリで Claude Code が作業するときのガイド。

## リポジトリ概要

セキュリティインシデント対応のツールキット置き場。

- `CHECKLIST.md` — Vercel OAuth (Context.ai) インシデントの対応チェックリスト
- `scan-secrets.sh` — ローカル/リポジトリの秘密情報残留スキャナ
- `.claude/skills/` — Claude Code 用スキル

## ライティング規約 (Fable 5 Writing Style)

このリポジトリでのすべての文章出力(回答・レポート・ドキュメント・PR 説明・
コミットメッセージ)には `.claude/skills/fable5-writing` のスキルを適用する。
まとまった文章を書く前にスキルを読み込むこと。

要点(詳細はスキル本体を参照):

1. **結論ファースト** — 最初の一文で「何が起きたか」に答える。経緯は後。
2. **読みやすさ > 短さ** — 短縮は情報の取捨選択で行い、載せる情報は完全な文で書く。
   矢印チェーン(`A → B → 失敗`)、断片文、未定義の略語は使わない。
3. **形式は質問に合わせる** — 単純な質問に見出しやセクションを被せない。
   表は短く列挙できる事実にだけ使い、説明は文章に書く。
4. **忠実な報告** — 失敗は失敗と出力付きで書く。完了は言い切る。ぼかさない。
5. **コメントは制約のみ** — コードコメントは「コードが示せない制約」だけに使う。

ドキュメントの言語はこのリポジトリの慣例に合わせて日本語を基本とする。
コード・コマンド・識別子は原文のまま英語で書く。

## 作業規約

- コミットメッセージは英語・命令形の一行サマリ + 必要なら本文(結論ファーストで)。
- 破壊的な操作(履歴書き換え、強制 push)は事前にユーザーへ確認する。
