# マルチモデル・オーケストレーション方針

このプロジェクトでは、**スピード（Sonnet 5）と品質（Opus）を役割分担で両立**する。
メインループは Sonnet 5 で高速に回し、判断の重い工程だけ Opus サブエージェントに委譲する。
迷ったら下の「モデル・ルーティング表」に従うこと。

## モデル・ルーティング表

| 工程 | モデル | 理由 |
|------|--------|------|
| コードベース調査・ファイル探索 | Haiku（`scout` エージェント） | 読むだけの作業は最速・最安で十分 |
| 実装・テスト作成・通常の修正 | Sonnet 5（メインループ） | 速度と品質のバランスが最良 |
| アーキテクチャ設計・実装計画 | Opus（`architect` エージェント） | 設計ミスは後工程全部に波及するため品質優先 |
| 最終コードレビュー | Opus（`deep-reviewer` エージェント） | 出荷前の最後の砦。見落としコストが高い |
| 2回失敗したバグの根本原因分析 | Opus（`debugger` エージェント） | 難所にだけ最高の推論力を投入する |

**原則: Opus は「診断と判断」に使い、手を動かすのは Sonnet 5。**
Opus サブエージェントには分析・計画・レビューを返させ、その結果をもとにメインループが実装する。
これが最もトークン効率が良く、速度も落ちない。

## 標準パイプライン

タスクの規模で分岐する:

### 小タスク（1〜2ファイルの修正、明確な指示）
サブエージェント不要。メインループ（Sonnet 5）で直接実装 → 検証 → コミット。
オーケストレーションのオーバーヘッドをかけないこと。

### 中〜大タスク（複数ファイル、設計判断あり、曖昧さあり）
1. **Scout（並列・Haiku）**: 関連コードの調査を `scout` エージェントに投げる。
   調査観点が複数あるなら、**1メッセージで複数の scout を同時起動**して並列化する。
2. **Plan（Opus）**: scout の結果を渡して `architect` に実装計画を立てさせる。
   計画には「変更ファイル一覧・手順・検証方法・リスク」を含めさせる。
3. **Implement（Sonnet 5・メインループ）**: 計画に沿って自分で実装する。
   独立した変更はまとめて進め、逐一報告や確認で止まらない。
4. **Verify（自動）**: テスト・lint・ビルドを実行。失敗したら質問せず修正して再実行。
5. **Review（Opus）**: 差分を `deep-reviewer` に渡して敵対的レビュー。
   指摘が CONFIRMED のものだけ修正し、再度 Verify。
6. **Commit**: 検証が通ったらコミット。

### エスカレーション・ルール
- 同じテスト失敗・同じバグに対する修正が **2回失敗したら**、自力で粘らず
  `debugger`（Opus）に「症状・試したこと・関連コード」を渡して根本原因分析させる。
- 設計の分岐で迷ったら（例: どちらのアプローチも妥当に見える）、`architect` に
  両案を比較・裁定させる。ユーザーへの質問は最後の手段。

## 自律行動の規範

- 変更後は必ずテスト・lint を実行して**自分で**確認する。「たぶん動く」で終えない。
- 失敗は質問せずに原因調査 → 修正 → 再実行。ただし2回ルール（上記）を守る。
- 妥当性が同程度の選択肢は片方を選んで進め、選択理由を最後に一言報告する。
- ユーザーに質問してよいのは: 破壊的操作の前、要件そのものが曖昧なとき、
  スコープが明らかに変わるときのみ。
- 作業完了の条件: 実装済み・検証済み・レビュー指摘対応済み・コミット済み。

## スピードのための規則

- **並列化を常に検討する**: 独立した調査・レビューは同一メッセージで複数エージェントを起動。
- **サブエージェントに生データを持ち帰らせない**: 結論と根拠の要約だけ返させ、
  メインのコンテキストを汚さない（コンテキスト肥大は速度と品質の両方を落とす）。
- **Opus を待ち時間にしない**: architect が計画中でも、確定している準備作業
  （ブランチ作成、依存インストール、既存テストのベースライン実行）は先に進める。
- Opus 呼び出しは1タスクで多くても 2〜3回（Plan / Debug / Review）。
  それ以上呼びたくなったら、タスク分割が悪いサイン。

## サブエージェント定義

`.claude/agents/` に定義済み。モデル指定は各定義ファイルの frontmatter で行う:

| エージェント | モデル | 役割 |
|--------------|--------|------|
| `scout` | haiku | 読み取り専用のコードベース調査 |
| `architect` | opus | 設計・実装計画・技術判断の裁定 |
| `deep-reviewer` | opus | 差分の敵対的コードレビュー |
| `debugger` | opus | 難バグの根本原因分析（読み取り専用） |

## 検証コマンド

<!-- プロジェクトに合わせて必ず埋めること。検証方法を知らないエージェントは自律的に動けない -->
- 構文チェック: `bash -n scan-secrets.sh`
- Lint: `shellcheck scan-secrets.sh`（未インストールなら導入してよい）
- 動作確認: `./scan-secrets.sh .` を実行し、exit code と出力を確認する
  （ripgrep `rg` が必要。テストスイートは未整備なので、スクリプトを変更したら実際に実行して確認すること）
